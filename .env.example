# Copy this file to .env.local and fill in values for local development.
# Vite exposes variables prefixed with VITE_. Restart dev server after changes.

# GitHub OAuth App Client ID (required for device flow sign-in)
VITE_GITHUB_CLIENT_ID=

# SurrealDB (used for app persistence/secrets). If not running, app will degrade gracefully.
VITE_SURREAL_URL=http://localhost:8000/sql
VITE_SURREAL_NS=app
VITE_SURREAL_DB=squadaid
# Basic auth (either provide VITE_SURREAL_AUTH as base64 user:pass, or set user/pass below)
# VITE_SURREAL_AUTH=
# VITE_SURREAL_USER=
# VITE_SURREAL_PASS=
# Basic auth is base64(username:password). Leave empty if not needed.
VITE_SURREAL_AUTH=
